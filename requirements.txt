# AeroTrack Analytics — Dependencias del pipeline ELT + webapp
# Instalar con: pip install -r requirements.txt

# ── Pipeline ELT ─────────────────────────────────────────────
python-dotenv>=1.0.0
pandas>=2.0.0
pyarrow>=14.0.0
minio>=7.2.0
requests>=2.31.0
tqdm>=4.66.0

# ── Web app FastAPI ───────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# ── Seguridad JWT ─────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
